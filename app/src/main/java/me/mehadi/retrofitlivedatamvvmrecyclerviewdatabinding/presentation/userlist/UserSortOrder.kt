package me.mehadi.retrofitlivedatamvvmrecyclerviewdatabinding.presentation.userlist

/** Session-only sort options for the Home user list; deliberately not persisted. */
enum class UserSortOrder {
    NAME,
    USERNAME,
    COMPANY,
}
